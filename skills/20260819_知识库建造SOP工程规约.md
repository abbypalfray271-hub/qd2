# 个人通用知识库建造与试卷解析 SOP 工程规约 (v2.0)

> [!NOTE]
> **摘要**：本工程规约总结自青岛中考语文真题库与 27 套区县模拟试卷的工程重构实践，确立了现代化中考语文试卷全量解析、原子化解构、高保真渲染与标准化存储的最高黄金 SOP 规约 (Exam Paper Parsing Skill v2.0)。

---

## 🏗 1. 知识库四分法标准架构 (4-Layer Container Architecture)

所有知识库项目根目录下必须设立且仅设立 4 个核心功能容器：

```
[知识库根目录]/
├── raw/         # 原始素材库（只读，防污染层）
├── wiki/        # 维基知识卡片库（AI 维护层）
├── outputs/     # 二次产出物（成果交付层）
├── skills/      # 工程规约与技能脚本（方法论沉淀层）
└── readme.md    # 知识库物理法则与主入口
```

### 1.1 容器职责与读写法则

| 目录 | 物理定位 | 读写权限 | 允许存放的内容 |
| :--- | :--- | :--- | :--- |
| **`raw/`** | 原始素材库 | **AI 只读 (Read Only)** | 未加工的原始文档、PDF、WORD、图片、考题、网页抓取等 |
| **`wiki/`** | 维基知识卡片库 | **AI 专属维护 (Read & Write)** | 原子化知识卡片 (`.md`)、全局动态索引 (`INDEX.md`) |
| **`outputs/`** | 成果交付层 | **按需生成 (Write Only)** | 提纯后的专题讲义、复习卷、分析报告、导出汇总包 |
| **`skills/`** | 方法论沉淀层 | **版本控制 (Read & Write)** | SOP 规约文档、清洗处理协议、转换/数据处理脚本 |

---

## 🎯 2. 中考语文试卷解析与解构黄金六法则 (Exam Paper Parsing Skill v2.0)

在解析、清洗、解构与渲染中考语文试卷时，必须 100% 严格遵循以下六大黄金法则：

### 2.1 组合大题精细解构法则 (Deconstruction Rule)
- **严禁打混大块**：严禁将包含小问 `（1）` `（2）` `（3）` `（4）` 的综合语段题或阅读大题直接打包为单道巨型题目。
- **原子化拆解**：必须按具体小问彻底精细解构拆分（如语段基础拆为 4 道独立选择题），使解构题块数量达到青岛中考标准规范（全卷 **24 ~ 30 道独立小题**）。

### 2.2 背景材料精准对齐法则 (Passage Alignment Rule)
- **独立题目（研学、默写、名著、写作）**：`passage` 字段必须置为空字符串 `""`，严禁让无关语段跨题浮现、张冠李戴！
- **阅读/语段题目**：`passage` 必须精准绑定对应的上下文阅读材料。

### 2.3 高保真着重号加点字法则 (Dot Emphasis Rule)
- 当题干包含“文中加点字”或“文中加点词语”时，背景材料 `passage` 中对应的考查字词**必须包裹高保真 HTML 加点标签** `<span class="dot-char">字</span>`。
- 严禁漏加点，在网页/APP 中必须完美呈现汉字下方规范的黑色中考着重点 `●`。

### 2.4 题干去污染法则 (Stem Cleaning Rule)
- 题干 `stem` 必须极其纯洁，仅包含题目要求说明与显式分值标记 `（X分）`（例如：`1. 文中加点字注音不正确的一项是（2分）（   ）`）。
- **严禁选项污染**：绝对禁止将选项文本 `A. ... B. ... C. ... D. ...` 误拼在 `stem` 末尾造成题目重复渲染与污染。

### 2.5 选项、答案与【解析】三位一体法则 (Option-Answer-Analysis Rule)
- **`options`**：数组独立收纳 A、B、C、D 规范选项。
- **`answer`**：明确标注正确选项字母或标准得分要点。
- **`analysis`**：统一格式化输出 **【答案】** 与 **【解析】**（包含考点定位、选项错误点剖析或解题步骤）。

### 2.6 三大模块 120 分总分对齐法则 (120-Point Calibration Rule)
- 试卷全卷算式必须符合青岛中考标准：
  - **一、积累与运用**：20 ~ 26 分
  - **二、阅读**：44 ~ 50 分
  - **三、写作**：50 分（作文选做保护，算式封顶 50 分）
  - **全卷满分总计**：**120 分**。

---

## 📝 3. 维基卡片与数据库构建法则 (Wiki & JSON Standards)

1. **时间戳强命名**：文件统一为 `YYYYMMDD_主题名称.md`。
2. **原子化与单点突破**：遵循一题一卡或一课一卡原则。
3. **双向索引**：更新 `INDEX.md` 索引网络，实现全库无缝关联。
